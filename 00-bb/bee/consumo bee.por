programa {
  funcao inicio() {
    real km,litros, consumo
    escreva("Distancia percorrida(KM):")
    leia(km)
    escreva("Total de combustivel gasto (L):")
    leia(litros)
    consumo = km /litros
    escreva(consumo)
  }
}
